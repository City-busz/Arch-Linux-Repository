# Maintainer: György Balló <ballogy@freestart.hu>
pkgname=gnome-web-photo
pkgver=0.10
pkgrel=1
pkgdesc="Create snapshot images of and print web pages from the command line"
arch=('i686' 'x86_64')
url="http://git.gnome.org/browse/gnome-web-photo/"
license=('LGPL')
depends=('libwebkit>=1.1.23' 'gconf')
makedepends=('intltool>=0.35.0')
install=$pkgname.install
source=(http://ftp.gnome.org/pub/GNOME/sources/$pkgname/$pkgver/$pkgname-$pkgver.tar.bz2)
sha256sums=('6421e460421f54e8a1b90df311726c6927032fe8c84fa86e39f5f4f8872363ea')

build() {
  cd "$srcdir/$pkgname-$pkgver"

  ./configure --prefix=/usr --sysconfdir=/etc --localstatedir=/var \
              --disable-static --disable-schemas-install \
              --with-gtk=2.0 --with-gconf-schema-file-dir=/usr/share/gconf/schemas
  make
}

package() {
  cd "$srcdir/$pkgname-$pkgver"

  make DESTDIR="$pkgdir/" install
}
